name: ActionFun Build

on: [push]

jobs:
  build:

    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: CD
      run: cd
    - name: Run cmake
      run: cmake [options] -S . -B build
    - name: Run make
      run: make
